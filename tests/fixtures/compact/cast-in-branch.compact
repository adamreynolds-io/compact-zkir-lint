pragma language_version >= 0.22.0;

ledger value: Uint<8>;

export circuit set_clamped(x: Uint<32>): [] {
  if (disclose(x) < 256) {
    value = disclose(x) as Uint<8>;
  } else {
    value = 255;
  }
  return [];
}
